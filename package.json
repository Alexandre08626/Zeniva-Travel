{
  "name": "zeniva-root",
  "private": true,
  "scripts": {
    "postinstall": "cd web && npm install --legacy-peer-deps",
    "vercel-build": "cd web && npm run build",
    "build": "cd web && npm run build",
    "dev": "cd web && npm run dev"
  },
  "dependencies": {
    "next": "16.1.6"
  }
}
