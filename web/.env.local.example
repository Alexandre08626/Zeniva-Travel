# Copy this file to `.env.local` and fill real secret values.
# DO NOT commit `.env.local` to git.

# Dev server port (optional)
PORT=3000

# Duffel API (Flight & Hotel search)
DUFFEL_API_URL=https://api.duffel.com
DUFFEL_API_KEY=your_duffel_api_key_here
DUFFEL_STAYS_API_KEY=your_duffel_stays_api_key_here
DUFFEL_VERSION=v2

# OpenAI API (Chat functionality)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
OPENAI_API_BASE=https://api.openai.com/v1

# Amadeus API (Alternative flight search)
AMADEUS_API_KEY=your_amadeus_api_key_here
AMADEUS_API_SECRET=your_amadeus_api_secret_here

# Lina / Chat server (if applicable)
LINA_API_URL=https://your-lina-server.example.com
LINA_API_KEY=your_lina_api_key_here

# Stripe (Payment processing)
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_PUBLIC_KEY=pk_test_XXXXXXXX

# Amadeus
AMADEUS_BASE_URL=https://test.api.amadeus.com
AMADEUS_CLIENT_ID=YOUR_AMADEUS_CLIENT_ID
AMADEUS_CLIENT_SECRET=YOUR_AMADEUS_CLIENT_SECRET

# Notes:
# - Place real secret keys in `.env.local` (not committed).
# - The local web UI currently talks to `/api/chat` which is mocked.
#   To point the UI to a real Lina server, update the API route or
#   set `LINA_API_URL` and implement server-side forwarding.
# Copy to .env.local if you plan to enable OpenAI later.
# Currently the app uses a mocked `/chat` route for visual work.
# To re-enable OpenAI, set your key here and update `app/chat/route.ts`.
OPENAI_API_KEY=
# Optional: override model (example: gpt-4o-mini)
OPENAI_MODEL=gpt-3.5-turbo

# Partner image API (optional): when set, image helpers will call this API
# Expected response shape: an array of image URLs (or { images: [...] } or { data: [...] })
PARTNER_API_URL=
PARTNER_API_KEY=

# LiteAPI (Nuitee) (server-side only)
# Keep these secrets on the server and call via `/api/partners/liteapi`.
LITEAPI_API_BASE_URL=https://api.liteapi.travel/v3.0
LITEAPI_API_KEY=
# Auth formatting (LiteAPI v3 docs): X-API-Key: <key>
LITEAPI_API_KEY_HEADER=X-API-Key
LITEAPI_API_KEY_PREFIX=

# LiteAPI booking host (PREBOOK/BOOK). Optional override.
LITEAPI_BOOK_BASE_URL=https://book.liteapi.travel/v3.0

# Expedia Group (server-side only)
# Keep these secrets on the server and call via `/api/partners/expedia`.
EXPEDIA_API_BASE_URL=https://example.expediagroup.com
EXPEDIA_API_KEY=
# Optional, depending on your Expedia product
EXPEDIA_API_SECRET=
# How the key is sent to Expedia (defaults to x-api-key)
EXPEDIA_API_KEY_HEADER=x-api-key
# Optional prefix like "Bearer" (leave empty if not needed)
EXPEDIA_API_KEY_PREFIX=

# Optional: Geocoding fallback (used by /api/partners/duffel-stays)
GEOCODING_API_KEY=
